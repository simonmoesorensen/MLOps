from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Classify the mnist dataset using a deep learning',
    author='simon moe sørensen',
    license='MIT',
)
